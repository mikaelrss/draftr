import React from 'react';
import { JellyfishSpinner } from 'react-spinners-kit';
import { SECONDARY } from '../../styles/colors';

const Spinner = () => <JellyfishSpinner loading={true} color={SECONDARY} />;

export default Spinner;
